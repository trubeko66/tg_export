# Telegram Channel Exporter

Программа для автоматического мониторинга и экспорта Telegram каналов с красивым псевдографическим интерфейсом.

## Возможности

- 🔍 **Мониторинг каналов** — автоматическое отслеживание новых сообщений
- 📱 **Поддержка медиафайлов** — загрузка фото, документов и других файлов
- 📊 **Множественные форматы экспорта** — JSON, HTML, Markdown
- 🚫 **Фильтрация контента** — исключение рекламы и промо-материалов IT-школ
- 🎯 **Улучшенный выбор каналов** — постраничный интерфейс, поиск, мультивыбор
- 💾 **Импорт/Экспорт каналов** — сохранение и загрузка списка каналов в JSON
- 🗂 **Настраиваемый путь хранения** — файл со списком каналов можно сохранять в произвольное место
- 📦 **Каталог экспорта каналов** — укажите базовый каталог, куда сохраняются папки каналов
- ☁️ **WebDAV синхронизация** — автоматическая синхронизация списка каналов и архивов
- 📧 **Уведомления в Telegram** — бот отправляет уведомления о новых сообщениях и синхронизации
- ✅ **Проверка экспорта** — автоматическая верификация создания файлов экспорта
- 📈 **Отслеживание прогресса** — реальное время показывает текущий канал и количество обработанных сообщений

## Требования

- Python 3.8+
- Telegram API credentials (API ID и API Hash)
- Telegram Bot Token (опционально, для уведомлений)

## Установка

1. Клонируйте репозиторий или скачайте файлы
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Настройка

### 1. Получение Telegram API credentials

1. Перейдите на https://my.telegram.org/auth
2. Войдите в свой аккаунт Telegram
3. Перейдите в "API development tools"
4. Создайте новое приложение и получите:
   - API ID
   - API Hash

### 2. Создание бота для уведомлений (опционально)

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите Bot Token
4. Найдите свой Chat ID (можно использовать @userinfobot)

## Использование

### Запуск программы

```bash
python telegram_exporter.py
```

### Первый запуск

1. **Настройка конфигурации**: При первом запуске программа автоматически предложит настроить:
   - API ID и API Hash (получите на https://my.telegram.org/auth)
   - Номер телефона
   - Опционально: Bot Token и Chat ID для уведомлений
2. **Импорт/экспорт списка каналов (опционально)**:
   - Действия: `import` (загрузить из JSON), `export` (сохранить текущий список), `reset` (сбросить состояние проблемных каналов), `skip`
   - При включенном WebDAV можно подтянуть актуальный `.channels` с сервера перед импортом
   - Команда `reset` помогает решить проблемы с каналами, которые не экспортируются
3. **Выбор каналов**:
   - Постранично по 10 штук; навигация `p`/`n`
   - Команды выбора: `sa` (выделить страницу), `sd` (снять страницу), `x` (очистить выделение)
   - Поиск: `f`, затем введите строку поиска по названию/username
   - Точечный и диапазонный выбор: `1,3-6` — переключает отметку у указанных номеров
   - `s` — продолжить, `q` — выход без выбора

### Управление конфигурацией

Для изменения настроек используйте:
```bash
python3 config.py
```

Или при запуске программы ответьте "да" на вопрос об изменении конфигурации.

### Повторный запуск

При повторном запуске программа предложит использовать сохранённый список каналов из файла по пути, заданному в настройках (по умолчанию `.channels`).

## Структура файлов

```
project/
├── telegram_exporter.py     # Основной файл программы
├── exporters.py            # Модуль экспортеров
├── config_manager.py       # Модуль управления конфигурацией
├── config.py              # Скрипт настройки конфигурации
├── start.py               # Скрипт запуска с проверкой зависимостей
├── requirements.txt       # Зависимости
├── README.md             # Документация
├── .config.json          # Конфигурация (создается автоматически)
├── .channels             # Сохраненные каналы (путь можно изменить в настройках)
├── export.log            # Логи работы программы
├── session_name.session  # Сессия Telegram (создается автоматически)
└── [Название канала]/    # Папки каналов
    ├── [Название канала].json    # JSON экспорт
    ├── [Название канала].html    # HTML экспорт
    ├── [Название канала].md      # Markdown экспорт
    └── media/                    # Медиафайлы
        ├── msg_123_photo.jpg
        └── msg_124_document.pdf
```

## Форматы экспорта

### JSON
Структурированные данные со всей информацией о сообщениях:
- ID сообщения
- Дата и время
- Текст
- Информация о медиа
- Статистика (просмотры, пересылки, ответы)

### HTML
Красиво оформленная веб-страница с:
- Адаптивным дизайном
- Градиентными заголовками
- Карточками сообщений
- Статистикой канала

### Markdown
Текстовый формат, совместимый с GitHub, Notion и другими платформами:
- Заголовки для каждого сообщения
- Экранированные специальные символы
- Ссылки на медиафайлы
- Статистика сообщений

## Интерфейс программы

### Выбор каналов

Программа предоставляет удобный интерфейс для выбора каналов:

1. **Постраничное отображение**: Каналы показываются по 10 штук на странице
2. **Навигация**: 
   - `p` - предыдущая страница
   - `n` - следующая страница  
   - `s` - перейти к выбору каналов
   - `q` - выход без выбора
3. **Поиск**: `f` — поиск по названию/username; `x` — очистка выбора
4. **Выбор**: `1,3-6` — переключение отметки у номеров и диапазонов; `sa`/`sd` — массовые действия

### Основной экран работы

Программа отображает статусный экран с информацией:

- **Заголовок**: Название программы и статус работы
- **Левая панель**: Список выбранных каналов с информацией о последней проверке
- **Правая панель**: Статистика экспорта (количество каналов, сообщений, объем данных)
- **Подвал**: Инструкции по завершению работы (Ctrl+C)

## Логирование

Все операции записываются в файл `export.log` с информацией:
- Время операции
- Уровень сообщения (INFO, ERROR, WARNING)
- Описание операции
- Объем загруженных данных
- Ошибки и их причины

## Уведомления

При настройке бота программа отправляет уведомления о:
- Новых сообщениях в каналах
- Успешном/неуспешном экспорте
- Успешной синхронизации WebDAV (если включены уведомления)
- Краткой аннотации постов
- Времени операции

Пример уведомления:
```
📢 Новые сообщения в канале

🔗 Канал: Название канала
📊 Новых сообщений: 5
📅 Время: 2024-01-15 12:30:45
✅ Статус: Успешно экспортировано

📁 Файлы сохранены в папку: Название канала
```

## Планировщик

Программа автоматически проверяет все каналы каждый день в 00:00 местного времени. Между проверками программа находится в режиме ожидания, отображая актуальную статистику.

## Обработка ошибок

- **Ограничения API**: Автоматическое ожидание при FloodWait ошибках
- **Сетевые ошибки**: Повторные попытки подключения
- **Ошибки медиа**: Пропуск проблемных файлов с логированием
- **Недоступные каналы**: Уведомление об ошибках доступа

## Особенности

### Очистка текста
- Удаление анимированных эмодзи
- Сохранение базовых эмодзи
- Удаление проблемных Unicode символов
- Экранирование специальных символов в Markdown

### Безопасность файлов
- Очистка имен файлов от недопустимых символов
- Ограничение длины имен файлов
- Создание уникальных имен для медиафайлов

### Оптимизация
## Хранилище и синхронизация

### Путь хранения списка каналов
- Настраивается в `config.py` → пункт «Настроить путь хранения списка каналов»
- Поле `storage.channels_path` в `.config.json`

### Каталог экспорта каналов
- Настраивается в `config.py` в том же пункте
- Поле `storage.export_base_dir` в `.config.json` (по умолчанию `exports`)
- Папки каналов будут создаваться внутри этого каталога

### WebDAV
- Включается в `config.py` → «Настроить WebDAV синхронизацию»
- Параметры:
  - `enabled` — включение
  - `url`, `username`, `password` — доступ к серверу
  - `remote_path` — удалённый файл для `.channels`
  - `auto_sync` — автосинхронизация `.channels`
  - `notify_on_sync` — уведомления в Telegram при успешной синхронизации
  - `upload_archives` — загружать ZIP‑архивы папок каналов
  - `archives_remote_dir` — каталог на сервере для архивов

### Проверка экспорта

Программа автоматически проверяет, что файлы экспорта (JSON, HTML, Markdown) были успешно созданы после обработки сообщений. Если файлы не созданы, экспорт считается неудачным и отправляется уведомление об ошибке.

### Инкрементальный экспорт

Программа использует инкрементальный режим экспорта:
- **Новые сообщения добавляются** к существующим файлам вместо перезаписи
- **Дубликаты автоматически удаляются** по ID сообщения
- **Сообщения сортируются** по ID (старые сначала)
- **История сохраняется** - все предыдущие экспорты остаются доступными

Это позволяет сохранить полную историю канала и избежать потери данных при повторных экспортах.

### Отслеживание прогресса

В реальном времени отображается:
- Текущий экспортируемый канал
- Количество обработанных сообщений в формате "Обработано X из Y"
- Общий прогресс экспорта
- Автоматическая повторная проверка каналов с отображением общего количества сообщений

Эта информация отображается в статусном экране программы без лишних тегов форматирования.

### Устранение проблем экспорта

Если канал показывает 0 сообщений, но в логах указано, что сообщения найдены:

1. **Используйте команду `reset`** при запуске программы для сброса состояния проблемных каналов
2. **Проверьте логи** - программа автоматически диагностирует проблемы доступа
3. **Принудительный переэкспорт** - сброс `last_message_id` заставит программу экспортировать все сообщения заново

Программа автоматически определяет каналы с проблемами и предлагает их исправить.

## Управление

- **Ctrl+C**: Корректное завершение работы программы
- **Автосохранение**: Регулярное сохранение прогресса и настроек

## Устранение неполадок

### Ошибки авторизации
- Проверьте правильность API ID и API Hash
- Убедитесь в корректности номера телефона
- Проверьте интернет-соединение

### Ошибки доступа к каналам
- Убедитесь, что вы подписаны на канал
- Проверьте, не является ли канал приватным
- Некоторые каналы могут ограничивать доступ через API

### Проблемы с ботом
- Проверьте правильность Bot Token
- Убедитесь, что Chat ID корректен
- Начните диалог с ботом в Telegram

## Лицензия

Данная программа предоставляется "как есть" для образовательных и личных целей. Используйте в соответствии с Terms of Service Telegram.

## Поддержка

При возникновении проблем проверьте файл `export.log` для получения подробной информации об ошибках.
